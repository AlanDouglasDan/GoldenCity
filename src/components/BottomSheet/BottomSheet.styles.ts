import {StyleSheet} from 'react-native';
// import {palette} from 'core/styles/palette';

export default StyleSheet.create({
  modal: {
    borderTopRightRadius: 40,
    borderTopLeftRadius: 40,
    // backgroundColor: palette.BACKGROUND_CONTAINER,
  },
});
